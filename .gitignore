# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

bin/

# Contains sensitive information about the environment like AWS account id
deploy/aws/terragrunt-live/*/*/common_vars.yml

# terraform/terragrunt
.terragrunt-cache
.terraform.lock.hcl

# k8s manifests
deploy/aws-load-balancer-controller-service-account.yaml
deploy/service.yml
deploy/ingress.yml
deploy/deployment.yml

# others
deploy/load-balancer-role-trust-policy.json
deploy/iam_policy.json
