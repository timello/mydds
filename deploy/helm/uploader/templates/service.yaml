apiVersion: v1
kind: Service
metadata:
  name: {{ include "uploader.fullname" . }}
  labels:
    {{- include "uploader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uploader.selectorLabels" . | nindent 4 }}
