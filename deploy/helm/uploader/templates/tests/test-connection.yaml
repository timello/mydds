apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uploader.fullname" . }}-test-connection"
  labels:
    {{- include "uploader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uploader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
